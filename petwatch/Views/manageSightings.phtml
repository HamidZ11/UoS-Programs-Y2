<?php require('template/header.phtml'); ?>

    <!-- View file for the Manage Sightings page -->
    <div class="container py-5">
        <h2 class="text-center mb-4">Manage My Sightings</h2>

        <?php if (!empty($view->sightings)): ?>

            <!-- DESKTOP TABLE VIEW -->
            <div class="table-responsive d-none d-md-block">
                <table class="table table-striped table-hover align-middle text-center">
                    <thead class="table-dark">
                    <tr>
                        <th>Sighting ID</th>
                        <th>Pet Name</th>
                        <th>Description</th>
                        <th>Latitude</th>
                        <th>Longitude</th>
                        <th>Date Reported</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    <?php foreach ($view->sightings as $sighting): ?>
                        <tr>
                            <td><?= htmlspecialchars($sighting['sightingID']) ?></td>
                            <td><?= htmlspecialchars($sighting['petName'] ?? '—') ?></td>
                            <td><?= htmlspecialchars($sighting['description']) ?></td>
                            <td><?= htmlspecialchars($sighting['latitude'] ?? '—') ?></td>
                            <td><?= htmlspecialchars($sighting['longitude'] ?? '—') ?></td>
                            <td><?= htmlspecialchars($sighting['dateReported']) ?></td>

                            <!-- Edit and Delete buttons -->
                            <td>
                                <a href="index.php?page=editSighting&amp;sightingID=<?= urlencode($sighting['sightingID']) ?>"
                                   class="btn btn-sm btn-outline-warning mb-1">Edit</a>
                                <a href="index.php?page=deleteSighting&amp;sightingID=<?= urlencode($sighting['sightingID']) ?>"
                                   class="btn btn-sm btn-outline-danger"
                                   onclick="return confirm('Are you sure you want to delete this sighting?');">Delete</a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
            </div>

            <!-- MOBILE CARD VIEW -->
            <div class="d-block d-md-none">
                <?php foreach ($view->sightings as $sighting): ?>
                    <div class="card mb-3 shadow-sm">
                        <div class="card-body">

                            <!-- Pet name and IDs -->
                            <h5 class="card-title mb-1">🐾 <?= htmlspecialchars($sighting['petName'] ?? 'Unknown Pet') ?></h5>
                            <small class="text-muted">
                                Sighting ID #<?= htmlspecialchars($sighting['sightingID']) ?> •
                                Pet ID #<?= htmlspecialchars($sighting['petID'] ?? '—') ?>
                            </small>

                            <!-- Sighting details -->
                            <p class="mt-3 mb-1"><strong>Description:</strong> <?= htmlspecialchars($sighting['description']) ?></p>
                            <div class="text-muted small mb-3">
                                <div><strong>Latitude:</strong> <?= htmlspecialchars($sighting['latitude'] ?? '—') ?></div>
                                <div><strong>Longitude:</strong> <?= htmlspecialchars($sighting['longitude'] ?? '—') ?></div>
                                <div><strong>Date Reported:</strong> <?= htmlspecialchars($sighting['dateReported']) ?></div>
                            </div>

                            <!-- Actions -->
                            <div class="d-flex flex-wrap gap-2">
                                <a href="index.php?page=editSighting&amp;sightingID=<?= urlencode($sighting['sightingID']) ?>"
                                   class="btn btn-sm btn-outline-warning">Edit</a>
                                <a href="index.php?page=deleteSighting&amp;sightingID=<?= urlencode($sighting['sightingID']) ?>"
                                   class="btn btn-sm btn-outline-danger"
                                   onclick="return confirm('Are you sure you want to delete this sighting?');">Delete</a>
                            </div>

                        </div>
                    </div>
                <?php endforeach; ?>
            </div>

            <!-- If no sightings exist -->
        <?php else: ?>
            <div class="alert alert-info text-center mt-4">
                You haven’t reported any sightings yet.
            </div>
        <?php endif; ?>
    </div>

<?php require('template/footer.phtml'); ?>