<?php require('template/header.phtml'); ?>

    <!-- View file for the Manage Listings page -->
    <div class="container py-5">
        <h2 class="text-center mb-4">Manage My Listings</h2>

        <?php if (!empty($view->pets)): ?>

            <!-- DESKTOP TABLE VIEW -->
            <div class="table-responsive d-none d-md-block">
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-dark">
                    <tr>
                        <th>Pet ID</th>
                        <th>Image</th>
                        <th>Name</th>
                        <th>Type</th>
                        <th>Age</th>
                        <th>Description</th>
                        <th>Status</th>
                        <th>Date Added</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    <?php foreach ($view->pets as $pet): ?>
                        <tr>
                            <td><?= htmlspecialchars($pet->getPetID()) ?></td>
                            <td>
                                <?php if ($pet->getImagePath()): ?>
                                    <img src="<?= htmlspecialchars($pet->getImagePath()) ?>"
                                         width="60" height="60"
                                         style="object-fit:cover;border-radius:8px;">
                                <?php else: ?>
                                    <span class="text-muted">No image</span>
                                <?php endif; ?>
                            </td>
                            <td><?= htmlspecialchars($pet->getName()) ?></td>
                            <td><?= htmlspecialchars($pet->getType()) ?></td>
                            <td><?= htmlspecialchars($pet->getAge()) ?></td>
                            <td><?= htmlspecialchars($pet->getDescription()) ?></td>

                            <!-- Display status badge -->
                            <td>
                                <?php if (strtolower($pet->getStatus()) === 'missing'): ?>
                                    <span class="badge bg-danger">Missing</span>
                                <?php else: ?>
                                    <span class="badge bg-success">Found</span>
                                <?php endif; ?>
                            </td>

                            <td><?= htmlspecialchars($pet->getDateAdded()) ?></td>

                            <!-- Edit/Delete actions -->
                            <td>
                                <a href="index.php?page=editPet&petID=<?= urlencode($pet->getPetID()) ?>"
                                   class="btn btn-sm btn-outline-warning mb-1">Edit</a>
                                <a href="index.php?page=deletePet&petID=<?= urlencode($pet->getPetID()) ?>"
                                   class="btn btn-sm btn-outline-danger"
                                   onclick="return confirm('Delete this listing?');">Delete</a>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
            </div>

            <!-- MOBILE CARD VIEW -->
            <div class="d-block d-md-none">
                <?php foreach ($view->pets as $pet): ?>
                    <div class="card mb-3 shadow-sm">
                        <div class="card-body">

                            <!-- Pet image + info header -->
                            <div class="d-flex align-items-center mb-3">
                                <?php if ($pet->getImagePath()): ?>
                                    <img src="<?= htmlspecialchars($pet->getImagePath()) ?>" alt="Pet Image"
                                         class="rounded me-3"
                                         style="width: 80px; height: 80px; object-fit: cover;">
                                <?php endif; ?>

                                <div>
                                    <h5 class="card-title mb-0"><?= htmlspecialchars($pet->getName()) ?></h5>
                                    <small class="text-muted">
                                        Pet ID #<?= htmlspecialchars($pet->getPetID()) ?> •
                                        <?= htmlspecialchars($pet->getType()) ?> •
                                        Age <?= htmlspecialchars($pet->getAge()) ?>
                                    </small>
                                </div>
                            </div>

                            <!-- Pet details -->
                            <p class="card-text mb-2"><strong>Description:</strong> <?= htmlspecialchars($pet->getDescription()) ?></p>
                            <p class="mb-1"><strong>Status:</strong>
                                <?php if (strtolower($pet->getStatus()) === 'missing'): ?>
                                    <span class="badge bg-danger">Missing</span>
                                <?php else: ?>
                                    <span class="badge bg-success">Found</span>
                                <?php endif; ?>
                            </p>
                            <p class="mb-2"><strong>Date Added:</strong> <?= htmlspecialchars($pet->getDateAdded()) ?></p>

                            <!-- Actions -->
                            <div class="d-flex flex-wrap gap-2">
                                <a href="index.php?page=editPet&petID=<?= urlencode($pet->getPetID()) ?>"
                                   class="btn btn-sm btn-outline-warning">Edit</a>
                                <a href="index.php?page=deletePet&petID=<?= urlencode($pet->getPetID()) ?>"
                                   class="btn btn-sm btn-outline-danger"
                                   onclick="return confirm('Delete this listing?');">Delete</a>
                            </div>
                        </div>
                    </div>
                <?php endforeach; ?>
            </div>

            <!-- If user has no pets -->
        <?php else: ?>
            <p class="text-center text-muted">You haven’t added any pets yet.</p>
        <?php endif; ?>
    </div>

<?php require('template/footer.phtml'); ?>