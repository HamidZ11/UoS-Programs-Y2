<?php require 'Views/template/header.phtml'; ?>

<div class="max-w-6xl mx-auto mt-12 bg-white rounded-2xl shadow-lg p-8 border border-gray-100">
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-3xl font-bold text-gray-800 flex items-center gap-2">
            📚 Assignment Tracker
        </h1>
        <a href="index.php?action=add"
           class="inline-flex items-center gap-2 px-5 py-2.5 text-white font-semibold bg-gradient-to-r from-indigo-500 to-purple-500 rounded-full shadow-sm hover:shadow-md hover:from-indigo-600 hover:to-purple-600 transition-all">
           <span class="text-lg">＋</span> Add New Assignment
        </a>
    </div>

    <?php if (empty($assignments)): ?>
        <p class="text-gray-600">No assignments yet. Add one above!</p>
    <?php else: ?>
        <div class="overflow-x-auto rounded-xl border border-gray-200 shadow-md bg-white">
            <table class="w-full text-left border-collapse rounded-xl overflow-hidden">
                <thead class="bg-gray-100 text-gray-800 uppercase text-sm font-semibold tracking-wide shadow-sm">
                    <tr>
                        <th class="px-6 py-4">ID</th>
                        <th class="px-6 py-4">Module</th>
                        <th class="px-6 py-4">Title</th>
                        <th class="px-6 py-4">Description</th>
                        <th class="px-6 py-4">Due Date</th>
                        <th class="px-6 py-4">Status</th>
                        <th class="px-6 py-4">Brief</th>
                        <th class="px-6 py-4 text-center">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                <?php foreach ($assignments as $assignment): ?>
                    <tr class="hover:bg-indigo-50/50 transition-all duration-300 hover:scale-[1.01]">
                        <td class="px-6 py-4 text-gray-700 font-medium"><?= htmlspecialchars($assignment['id']); ?></td>
                        <td class="px-6 py-4 font-semibold text-gray-900"><?= htmlspecialchars($assignment['module']); ?></td>
                        <td class="px-6 py-4 text-gray-800"><?= htmlspecialchars($assignment['title']); ?></td>
                        <td class="px-6 py-4 text-gray-600"><?= htmlspecialchars($assignment['description']); ?></td>
                        <td class="px-6 py-4 font-semibold text-gray-800"><?= htmlspecialchars($assignment['due_date']); ?></td>
                        <td class="px-6 py-4">
                            <?php
                                $status = $assignment['status'];
                                $statusClasses = [
                                    'Done' => 'bg-green-100/80 text-green-800 shadow-sm',
                                    'In Progress' => 'bg-yellow-100/80 text-yellow-800 shadow-sm',
                                    'To Do' => 'bg-red-100/80 text-red-800 shadow-sm'
                                ];
                                $badgeClass = $statusClasses[$status] ?? 'bg-gray-100 text-gray-800';
                            ?>
                            <span class="px-3 py-1 rounded-full text-xs font-semibold <?= $badgeClass; ?> backdrop-blur-sm transition-all shadow-sm border border-white/40">
                                <?= htmlspecialchars($status); ?>
                            </span>
                        </td>
                        <td class="px-6 py-4 text-center">
                            <?php if (!empty($assignment['brief'])): ?>
                                <a href="<?= htmlspecialchars($assignment['brief']); ?>"
                                   target="_blank"
                                   class="inline-flex flex-col items-center justify-center px-4 py-2 rounded-lg text-sm font-medium bg-indigo-100 text-indigo-700 hover:bg-indigo-200 transition-all duration-200 shadow-sm hover:shadow">
                                   📄 <span class="mt-1">View</span>
                                </a>
                            <?php else: ?>
                                <span class="inline-flex flex-col items-center justify-center px-4 py-2 rounded-lg text-sm font-medium bg-gray-100 text-gray-400 cursor-not-allowed">
                                   📄 <span class="mt-1">No File</span>
                                </span>
                            <?php endif; ?>
                        </td>
                        <td class="px-6 py-4 flex justify-center items-center gap-3">
                            <a href="index.php?action=edit&id=<?= $assignment['id']; ?>"
                               class="inline-flex items-center gap-1 px-3 py-1.5 rounded-lg text-sm font-medium bg-yellow-100 text-yellow-800 hover:bg-yellow-200 transition-all duration-200 shadow-sm hover:shadow">
                               ✏️ Edit
                            </a>

                            <a href="index.php?action=delete&id=<?= $assignment['id']; ?>"
                               class="inline-flex items-center gap-1 px-3 py-1.5 rounded-lg text-sm font-medium bg-red-100 text-red-700 hover:bg-red-200 transition-all duration-200 shadow-sm hover:shadow"
                               onclick="return confirm('Are you sure you want to delete this assignment?');">
                               🗑️ Delete
                            </a>
                        </td>
                    </tr>
                <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    <?php endif; ?>
</div>

<?php require 'Views/template/footer.phtml'; ?>